{
  "name": "currensee",
  "private": true,
  "version": "1.0.0",
  "description": "CurrenSee — smart currency conversion (Flutter web). Vercel builds via scripts/vercel_build.sh.",
  "scripts": {
    "build": "bash scripts/vercel_build.sh"
  }
}
